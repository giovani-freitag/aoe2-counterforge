/// <reference types="vite/client" />

/** Version of the running build, stamped in from the manifest at build time. */
declare const __APP_VERSION__: string;

/** Address of the repository this build came from. */
declare const __APP_REPOSITORY__: string;
